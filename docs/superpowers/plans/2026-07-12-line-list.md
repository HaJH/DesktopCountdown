# 줄 리스트 + 토큰 템플릿 구현 계획 (계획 3)

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 요약줄/본문 2줄 고정 구조를 없애고, 토큰 템플릿을 담은 `[[line]]` 리스트로 화면을 구성한다. 설정 창에서 줄을 추가·삭제·정렬하고 프리셋을 적용할 수 있다.

**Architecture:** `Config`에 `lines: Vec<Line>`이 생기고, 각 `Line`은 `text`(토큰 템플릿) + `size_ratio` + `align` + `color?`를 가진다. 나머지 스타일은 `[style]` 공통. 렌더러는 `&[Line]`을 위에서 아래로 쌓고, `app.rs`가 매 틱 `tokens::render`로 텍스트를 만든다. 기존 `show_summary_line`은 읽기 전용 레거시 필드로 남아 첫 로드 때 기본 리스트를 합성하는 데만 쓰인다.

**Tech Stack:** Rust, `serde`/`toml`, `jiff`, `windows` (Direct2D/DirectWrite/DirectComposition), `eframe`/`egui`

**설계 문서:** `docs/superpowers/specs/2026-07-12-line-list-design.md`

## Global Constraints

- 새 크레이트 의존성을 추가하지 않는다.
- 코드·주석·설정 창 UI 문구는 영어. 문서(README, 계획, 설계)는 한국어.
- 커밋 메시지는 한국어, 간결하게(제목 + 불릿 몇 줄). 자동 생성 문구(Co-Authored-By 등) 금지.
- 각 태스크 끝에서 `cargo test`가 전부 통과해야 한다. 렌더러 테스트는 실제 Direct2D/DirectWrite를 쓰므로 Windows에서 실행된다.
- `deny_unknown_fields`가 걸려 있으므로 기존 `config.toml`의 모든 키가 계속 파싱되어야 한다 — `show_summary_line`을 지우지 말 것.
- 텍스트 상한 200자, `size_ratio > 0` (유한), 색은 `#RRGGBB`.
- 기본 설정의 화면은 지금과 동일해야 한다: `{months}m {weeks}w {days}d`(0.28) + `{hh}:{mm}:{ss}`(1.0).

---

### Task 1: 토큰 치환 (`src/tokens.rs`)

**Files:**
- Create: `src/tokens.rs`
- Modify: `src/lib.rs`

**Interfaces:**
- Consumes: `crate::countdown::Breakdown` (필드: `months`, `weeks`, `days`, `total_hours`, `minutes`, `seconds`, `expired` — 모두 `i64`/`bool`)
- Produces:
  - `pub fn render(template: &str, b: &Breakdown) -> String`
  - `pub const TOKENS: [(&str, &str); 13]` — (토큰, 영문 설명). 설정 창의 도움말 패널이 그대로 그린다.

- [ ] **Step 1: 실패하는 테스트를 쓴다**

`src/tokens.rs`를 만들고 본문은 테스트만 넣는다.

```rust
//! Token substitution for line templates. No Win32, no I/O.

use crate::countdown::Breakdown;

#[cfg(test)]
mod tests {
    use super::*;
    use jiff::{
        civil::datetime,
        tz::{offset, TimeZone},
        Zoned,
    };

    fn z(y: i16, m: i8, d: i8, h: i8, mi: i8, s: i8) -> Zoned {
        datetime(y, m, d, h, mi, s, 0)
            .to_zoned(TimeZone::fixed(offset(9)))
            .unwrap()
    }

    /// 2026-07-12 09:00 -> 2026-10-24 09:00 = 104 days = 2496 hours.
    fn b() -> Breakdown {
        crate::countdown::breakdown(&z(2026, 7, 12, 9, 0, 0), &z(2026, 10, 24, 9, 0, 0))
    }

    #[test]
    fn main_template_matches_the_old_clock_format() {
        assert_eq!(render("{hh}:{mm}:{ss}", &b()), "2496:00:00");
    }

    #[test]
    fn summary_template_matches_the_old_summary_format() {
        assert_eq!(render("{months}m {weeks}w {days}d", &b()), "3m 1w 5d");
    }

    #[test]
    fn zero_padding_applies_to_minutes_and_seconds() {
        let b = crate::countdown::breakdown(&z(2026, 10, 24, 8, 59, 53), &z(2026, 10, 24, 9, 0, 0));
        assert_eq!(render("{hh}:{mm}:{ss}", &b), "00:00:07");
        assert_eq!(render("{minutes}:{seconds}", &b), "0:7");
    }

    #[test]
    fn totals_are_derived_from_the_breakdown() {
        let b = b();
        assert_eq!(render("{daysTotal}", &b), "104");
        assert_eq!(render("{hoursTotal}", &b), "2496");
        assert_eq!(render("{minutesTotal}", &b), "149760");
        assert_eq!(render("{secondsTotal}", &b), "8985600");
        assert_eq!(render("{hours}", &b), "0");
    }

    #[test]
    fn unknown_tokens_are_left_alone_so_typos_are_visible() {
        assert_eq!(render("{dayz} left", &b()), "{dayz} left");
    }

    #[test]
    fn unmatched_braces_are_left_alone() {
        assert_eq!(render("100% {done", &b()), "100% {done");
        assert_eq!(render("a } b", &b()), "a } b");
    }

    #[test]
    fn plain_text_passes_through_including_non_ascii() {
        assert_eq!(render("수능까지 {daysTotal}일", &b()), "수능까지 104일");
    }

    #[test]
    fn expired_renders_zeroes() {
        let t = z(2026, 10, 24, 9, 0, 0);
        let b = crate::countdown::breakdown(&t, &t);
        assert_eq!(render("{hh}:{mm}:{ss} / {daysTotal}", &b), "00:00:00 / 0");
    }

    #[test]
    fn every_advertised_token_resolves() {
        let b = b();
        for (token, _) in TOKENS {
            assert_ne!(
                render(token, &b),
                token,
                "{token} is advertised but does not resolve"
            );
        }
    }
}
```

- [ ] **Step 2: 실패를 확인한다**

Run: `cargo test tokens`
Expected: 컴파일 실패 — `cannot find function 'render'`, `cannot find value 'TOKENS'`

- [ ] **Step 3: 구현한다**

`src/tokens.rs`의 테스트 모듈 위에 넣는다.

```rust
/// Replaces every `{token}` in `template` with its value from `b`.
///
/// An unknown token, or a `{` with no `}`, is copied through unchanged: a typo shows up on
/// the wallpaper instead of silently vanishing.
pub fn render(template: &str, b: &Breakdown) -> String {
    let mut out = String::with_capacity(template.len() + 16);
    let mut rest = template;
    while let Some(open) = rest.find('{') {
        out.push_str(&rest[..open]);
        let after = &rest[open + 1..];
        let Some(close) = after.find('}') else {
            out.push_str(&rest[open..]);
            return out;
        };
        let name = &after[..close];
        match value(name, b) {
            Some(v) => out.push_str(&v),
            None => {
                out.push('{');
                out.push_str(name);
                out.push('}');
            }
        }
        rest = &after[close + 1..];
    }
    out.push_str(rest);
    out
}

fn value(name: &str, b: &Breakdown) -> Option<String> {
    let total_minutes = b.total_hours * 60 + b.minutes;
    let total_seconds = total_minutes * 60 + b.seconds;
    Some(match name {
        "months" => b.months.to_string(),
        "weeks" => b.weeks.to_string(),
        "days" => b.days.to_string(),
        "daysTotal" => (b.total_hours / 24).to_string(),
        "hoursTotal" => b.total_hours.to_string(),
        "minutesTotal" => total_minutes.to_string(),
        "secondsTotal" => total_seconds.to_string(),
        "hours" => (b.total_hours % 24).to_string(),
        "minutes" => b.minutes.to_string(),
        "seconds" => b.seconds.to_string(),
        "hh" => format!("{:02}", b.total_hours),
        "mm" => format!("{:02}", b.minutes),
        "ss" => format!("{:02}", b.seconds),
        _ => return None,
    })
}

/// Every token, with the description the settings window shows next to it.
pub const TOKENS: [(&str, &str); 13] = [
    ("{months}", "whole calendar months left"),
    ("{weeks}", "whole weeks after the months"),
    ("{days}", "whole days after the weeks"),
    ("{daysTotal}", "total whole days left"),
    ("{hoursTotal}", "total whole hours left"),
    ("{minutesTotal}", "total whole minutes left"),
    ("{secondsTotal}", "total seconds left"),
    ("{hours}", "hours within the day (0-23)"),
    ("{minutes}", "minutes within the hour (0-59)"),
    ("{seconds}", "seconds within the minute (0-59)"),
    ("{hh}", "total hours, zero-padded to two digits"),
    ("{mm}", "minutes, zero-padded to two digits"),
    ("{ss}", "seconds, zero-padded to two digits"),
];
```

`src/lib.rs`에 모듈을 추가한다 (알파벳 순서 유지, `single_instance` 다음).

```rust
pub mod tokens;
```

- [ ] **Step 4: 통과를 확인한다**

Run: `cargo test tokens`
Expected: 9 tests PASS

- [ ] **Step 5: 커밋**

```bash
git add src/tokens.rs src/lib.rs
git commit -m "토큰 치환 함수 추가

- {months} {daysTotal} {hh} 등 13개 토큰
- 미정의 토큰과 짝 없는 중괄호는 그대로 통과"
```

---

### Task 2: 줄 리스트로 전환 (config → 렌더러 → 앱)

`Style::show_summary_line`의 타입이 바뀌는 순간 렌더러와 설정 창이 동시에 깨지므로, 이 셋을 한 태스크로 묶는다. 이 태스크가 끝나면 기본 설정의 바탕화면은 지금과 픽셀 단위로 같고, `config.toml`에 `[[line]]`이 나타난다. 설정 창의 줄 편집 UI는 Task 4에서 붙는다(이 태스크에서는 "Show summary line" 체크박스가 사라지고 그 자리에 아무것도 없다).

**Files:**
- Modify: `src/config/schema.rs`, `src/config/io.rs`, `src/config/merge.rs`, `src/render/mod.rs`, `src/app.rs`, `src/countdown.rs`, `src/settings/app.rs`, `src/settings/overrides.rs`

**Interfaces:**
- Produces:
  - `config::Align { Left, Center, Right }` (serde kebab-case)
  - `config::Line { text: String, size_ratio: f32, align: Align, color: Option<String> }` — `Default`: `("", 1.0, Center, None)`
  - `config::Config::lines: Vec<Line>` (TOML `[[line]]`)
  - `config::DisplayOverride::lines: Option<Vec<Line>>` (TOML `[[display.line]]`, 구조체 **맨 마지막 필드**)
  - `config::Style::show_summary_line: Option<bool>` — 레거시, `skip_serializing`
  - `config::default_lines(summary: bool) -> Vec<Line>`
  - `config::migrate(cfg: &mut Config)`
  - `config::Effective::lines: Vec<Line>`
  - `render::Painter::compose(&self, lines: &[Line], style: &Style) -> Result<Composed>`
  - `render::Painter::measure(&self, lines: &[Line], style: &Style) -> Result<(u32, u32)>`
  - `render::Lines` 구조체는 **삭제**된다.
  - `countdown::format_main`/`countdown::format_summary`는 **삭제**된다.

- [ ] **Step 1: 스키마 테스트를 쓴다 (실패)**

`src/config/schema.rs`의 `mod tests`에 추가한다.

```rust
    #[test]
    fn default_config_has_the_classic_two_lines() {
        let cfg = Config::default();
        assert_eq!(cfg.lines.len(), 2);
        assert_eq!(cfg.lines[0].text, "{months}m {weeks}w {days}d");
        assert_eq!(cfg.lines[0].size_ratio, 0.28);
        assert_eq!(cfg.lines[0].align, Align::Center);
        assert_eq!(cfg.lines[0].color, None);
        assert_eq!(cfg.lines[1].text, "{hh}:{mm}:{ss}");
        assert_eq!(cfg.lines[1].size_ratio, 1.0);
    }

    #[test]
    fn lines_are_parsed_from_an_array_of_tables() {
        let cfg: Config = toml::from_str(
            r#"
target = "2026-10-24T09:00:00"

[[line]]
text = "수능까지"
size_ratio = 0.25
align = "right"
color = "#AABBCC"

[[line]]
text = "{hh}:{mm}:{ss}"
"#,
        )
        .unwrap();
        assert_eq!(cfg.lines.len(), 2);
        assert_eq!(cfg.lines[0].text, "수능까지");
        assert_eq!(cfg.lines[0].align, Align::Right);
        assert_eq!(cfg.lines[0].color.as_deref(), Some("#AABBCC"));
        // Defaults fill the second line.
        assert_eq!(cfg.lines[1].size_ratio, 1.0);
        assert_eq!(cfg.lines[1].align, Align::Center);
        assert_eq!(cfg.lines[1].color, None);
    }

    #[test]
    fn a_display_override_can_replace_the_whole_line_list() {
        let cfg: Config = toml::from_str(
            r#"
target = "2026-10-24T09:00:00"

[[display]]
id = "MON-A"
size_px = 48.0

[[display.line]]
text = "D-{daysTotal}"
"#,
        )
        .unwrap();
        let lines = cfg.displays[0].lines.as_ref().unwrap();
        assert_eq!(lines.len(), 1);
        assert_eq!(lines[0].text, "D-{daysTotal}");
    }

    #[test]
    fn legacy_show_summary_line_still_parses_but_is_not_written_back() {
        let cfg: Config = toml::from_str(
            "target = \"2026-10-24T09:00:00\"\n[style]\nshow_summary_line = false\n",
        )
        .unwrap();
        assert_eq!(cfg.style.show_summary_line, Some(false));

        let text = toml::to_string_pretty(&cfg).unwrap();
        assert!(
            !text.contains("show_summary_line"),
            "the legacy flag must not be serialized: {text}"
        );
    }

    #[test]
    fn validate_rejects_nonpositive_size_ratio() {
        let mut cfg = Config::default();
        cfg.lines[0].size_ratio = 0.0;
        assert!(matches!(validate(&cfg), Err(ConfigError::SizeRatio(_))));
    }

    #[test]
    fn validate_rejects_bad_line_colour() {
        let mut cfg = Config::default();
        cfg.lines[0].color = Some("nope".into());
        assert!(matches!(validate(&cfg), Err(ConfigError::Color(_))));
    }

    #[test]
    fn validate_rejects_overlong_line_text() {
        let mut cfg = Config::default();
        cfg.lines[0].text = "가".repeat(MAX_TEXT_LEN + 1);
        assert!(matches!(validate(&cfg), Err(ConfigError::TextTooLong(_))));
    }

    #[test]
    fn validate_checks_lines_in_display_overrides_too() {
        let mut cfg = Config::default();
        cfg.displays.push(DisplayOverride {
            id: "MON-A".into(),
            lines: Some(vec![Line {
                size_ratio: -1.0,
                ..Line::default()
            }]),
            ..DisplayOverride::default()
        });
        assert!(matches!(validate(&cfg), Err(ConfigError::SizeRatio(_))));
    }

    #[test]
    fn migrate_synthesizes_the_classic_list_for_a_config_without_lines() {
        let mut cfg: Config = toml::from_str(MINIMAL).unwrap();
        cfg.lines.clear();
        migrate(&mut cfg);
        assert_eq!(cfg.lines, default_lines(true));
    }

    #[test]
    fn migrate_honours_the_legacy_summary_flag() {
        let mut cfg: Config = toml::from_str(
            "target = \"2026-10-24T09:00:00\"\n[style]\nshow_summary_line = false\n",
        )
        .unwrap();
        migrate(&mut cfg);
        assert_eq!(cfg.lines, default_lines(false));
        assert_eq!(cfg.lines.len(), 1);
        assert_eq!(cfg.lines[0].text, "{hh}:{mm}:{ss}");
    }

    #[test]
    fn migrate_leaves_an_existing_list_alone() {
        let mut cfg: Config = toml::from_str(
            r#"
target = "2026-10-24T09:00:00"
[style]
show_summary_line = false

[[line]]
text = "kept"
"#,
        )
        .unwrap();
        migrate(&mut cfg);
        assert_eq!(cfg.lines.len(), 1);
        assert_eq!(cfg.lines[0].text, "kept");
    }

    #[test]
    fn migrate_converts_a_legacy_per_monitor_summary_flag_into_a_line_list() {
        let mut cfg: Config = toml::from_str(
            r#"
target = "2026-10-24T09:00:00"

[[display]]
id = "MON-A"
show_summary_line = false
"#,
        )
        .unwrap();
        migrate(&mut cfg);
        assert_eq!(cfg.displays[0].lines, Some(default_lines(false)));
    }

    #[test]
    fn migrate_leaves_a_monitor_without_the_legacy_flag_following_the_globals() {
        let mut cfg: Config = toml::from_str(
            r#"
target = "2026-10-24T09:00:00"

[[display]]
id = "MON-A"
size_px = 48.0
"#,
        )
        .unwrap();
        migrate(&mut cfg);
        assert_eq!(cfg.displays[0].lines, None);
    }
```

기존 테스트 `minimal_config_fills_defaults`에서 `assert!(cfg.style.show_summary_line);` 줄을 지우고 대신 넣는다.

```rust
        assert_eq!(cfg.style.show_summary_line, None);
        assert!(cfg.lines.is_empty(), "a file without [[line]] parses as empty; migrate fills it");
```

- [ ] **Step 2: 실패를 확인한다**

Run: `cargo test --lib config::schema`
Expected: 컴파일 실패 — `Align`, `Line`, `MAX_TEXT_LEN`, `migrate`, `default_lines` 없음

- [ ] **Step 3: 스키마를 구현한다**

`src/config/schema.rs`:

기본값 함수들 아래에 추가한다.

```rust
fn d_size_ratio() -> f32 {
    1.0
}
fn d_align() -> Align {
    Align::Center
}

/// The summary line's em size as a fraction of `size_px`. Was hard-coded in the renderer
/// as `SUMMARY_RATIO` before lines became configurable; kept as the classic preset's value.
pub const SUMMARY_SIZE_RATIO: f32 = 0.28;
pub const SUMMARY_TEMPLATE: &str = "{months}m {weeks}w {days}d";
pub const MAIN_TEMPLATE: &str = "{hh}:{mm}:{ss}";
/// Longest `Line::text` accepted, in characters.
pub const MAX_TEXT_LEN: usize = 200;

#[derive(Debug, Clone, Copy, PartialEq, Eq, Deserialize, Serialize)]
#[serde(rename_all = "kebab-case")]
pub enum Align {
    Left,
    Center,
    Right,
}

/// One line of the countdown. `text` is a token template (see `crate::tokens`); everything
/// not named here (font, weight, draw mode, outline, shadow, letter spacing, opacity) comes
/// from `[style]` and is shared by every line.
#[derive(Debug, Clone, PartialEq, Deserialize, Serialize)]
#[serde(deny_unknown_fields)]
pub struct Line {
    #[serde(default)]
    pub text: String,
    /// Em size as a fraction of `Style::size_px`.
    #[serde(default = "d_size_ratio")]
    pub size_ratio: f32,
    #[serde(default = "d_align")]
    pub align: Align,
    /// `None` inherits `Style::color`.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub color: Option<String>,
}

impl Default for Line {
    fn default() -> Self {
        Self {
            text: String::new(),
            size_ratio: d_size_ratio(),
            align: d_align(),
            color: None,
        }
    }
}

/// The classic two-line countdown: an optional summary above the clock.
pub fn default_lines(summary: bool) -> Vec<Line> {
    let mut v = Vec::with_capacity(2);
    if summary {
        v.push(Line {
            text: SUMMARY_TEMPLATE.to_string(),
            size_ratio: SUMMARY_SIZE_RATIO,
            ..Line::default()
        });
    }
    v.push(Line {
        text: MAIN_TEMPLATE.to_string(),
        ..Line::default()
    });
    v
}
```

`Style`에서 `show_summary_line` 필드를 바꾼다.

```rust
    /// Legacy. Older config files carried this flag instead of a `[[line]]` list; `migrate`
    /// turns it into one and nothing else reads it. Never written back (`skip_serializing`),
    /// but still accepted on read — `deny_unknown_fields` would reject an existing file
    /// otherwise.
    #[serde(default, skip_serializing)]
    pub show_summary_line: Option<bool>,
```

`impl Default for Style`에서 `show_summary_line: true` → `show_summary_line: None`.

`DisplayOverride`에서 `show_summary_line` 필드에 같은 주석/속성을 달고(`Option<bool>`은 이미 맞다) `#[serde(default, skip_serializing)]`로 바꾼 뒤, **구조체 맨 끝**에 `lines`를 추가한다. TOML은 테이블 뒤에 스칼라를 쓸 수 없으므로 배열 필드가 마지막이어야 한다.

```rust
    #[serde(default, skip_serializing)]
    pub show_summary_line: Option<bool>,
    /// Replaces the global line list wholesale (no per-line merge). TOML: `[[display.line]]`.
    /// Must stay the last field: TOML rejects a scalar written after a table.
    #[serde(default, rename = "line", skip_serializing_if = "Option::is_none")]
    pub lines: Option<Vec<Line>>,
```

`Config`에 `lines`를 추가한다 (`displays` 바로 앞).

```rust
    #[serde(default, rename = "line", skip_serializing_if = "Vec::is_empty")]
    pub lines: Vec<Line>,
```

`impl Default for Config`에 `lines: default_lines(true),`를 넣는다.

`ConfigError`에 두 갈래를 추가한다.

```rust
    #[error("size_ratio must be greater than 0, got {0}")]
    SizeRatio(f32),
    #[error("line text must be at most 200 characters, got {0}")]
    TextTooLong(usize),
```

검사 함수와 `validate`:

```rust
fn check_lines(lines: &[Line]) -> Result<(), ConfigError> {
    for l in lines {
        if !(l.size_ratio.is_finite() && l.size_ratio > 0.0) {
            return Err(ConfigError::SizeRatio(l.size_ratio));
        }
        let len = l.text.chars().count();
        if len > MAX_TEXT_LEN {
            return Err(ConfigError::TextTooLong(len));
        }
        if let Some(c) = &l.color {
            check_color(c)?;
        }
    }
    Ok(())
}
```

`validate`의 `let s = &cfg.style;` 블록 뒤에 `check_lines(&cfg.lines)?;`를, `for d in &cfg.displays` 루프 안 끝에 다음을 넣는다.

```rust
        if let Some(lines) = &d.lines {
            check_lines(lines)?;
        }
```

마이그레이션:

```rust
/// Fills in the `[[line]]` list for a config written before lines existed (or hand-edited to
/// have none). An empty list means "not configured", not "draw nothing" — the settings window
/// keeps at least one line, and a display is silenced with `enabled = false`, not by emptying
/// its list.
///
/// The legacy `show_summary_line` flag is the only thing that decides the synthesized list.
/// It is left in place afterwards (it is never serialized, so it cannot leak back into the
/// file) but nothing else reads it.
pub fn migrate(cfg: &mut Config) {
    if cfg.lines.is_empty() {
        cfg.lines = default_lines(cfg.style.show_summary_line.unwrap_or(true));
    }
    for d in &mut cfg.displays {
        if d.lines.is_none() {
            if let Some(summary) = d.show_summary_line {
                d.lines = Some(default_lines(summary));
            }
        }
    }
}
```

- [ ] **Step 4: 스키마 테스트 통과를 확인한다**

Run: `cargo test --lib config::schema`
Expected: PASS (다른 모듈은 아직 깨져 있으므로 `--lib config::schema`만 돌린다 — 컴파일이 안 되면 Step 5~9를 먼저 끝내고 한 번에 확인한다)

> 참고: Rust는 크레이트 전체를 컴파일하므로 이 시점에는 `render`/`settings`/`app`가 깨져 테스트가 아예 안 돈다. Step 5~9를 마친 뒤 `cargo test`로 한꺼번에 검증한다. Step 4는 "스키마 코드가 의도대로 쓰였는지" 눈으로 확인하는 단계로 두고 넘어간다.

- [ ] **Step 5: io / merge를 고친다**

`src/config/io.rs`의 `load_or_create`에서 파싱 직후 마이그레이션한다.

```rust
    let mut cfg: Config =
        toml::from_str(&text).with_context(|| format!("parsing {}", path.display()))?;
    super::migrate(&mut cfg);
    validate(&cfg)?;
    Ok(cfg)
```

같은 파일의 테스트에 추가한다.

```rust
    #[test]
    fn a_legacy_file_without_lines_gains_the_classic_list_on_load() {
        let p = tmp("legacy");
        fs::write(
            &p,
            "target = \"2030-01-01T00:00:00\"\n[style]\nshow_summary_line = false\n",
        )
        .unwrap();
        let cfg = load_or_create(&p).unwrap();
        assert_eq!(cfg.lines, crate::config::default_lines(false));
    }
```

`src/config/merge.rs`: `Effective`에 `lines`를 넣는다.

```rust
pub struct Effective {
    pub enabled: bool,
    pub anchor: Anchor,
    pub offset_px: [i32; 2],
    pub style: Style,
    pub lines: Vec<Line>,
}
```

`use super::{Anchor, Config, Line, Style};`로 import를 넓히고, `effective_for`의 초기화에 `lines: cfg.lines.clone(),`를 추가한 뒤, `show_summary_line` 오버라이드 블록(`if let Some(v) = o.show_summary_line { ... }`)을 **지우고** 그 자리에 넣는다.

```rust
    if let Some(v) = &o.lines {
        e.lines = v.clone();
    }
```

merge 테스트의 `every_style_field_can_be_overridden`에서 `show_summary_line: Some(false),`와 `assert!(!s.show_summary_line);`를 지우고, 새 테스트를 추가한다.

```rust
    #[test]
    fn no_override_yields_the_global_line_list() {
        let cfg = cfg_with(vec![]);
        assert_eq!(effective_for(&cfg, "MON-A").lines, cfg.lines);
    }

    #[test]
    fn a_line_override_replaces_the_whole_list() {
        let mine = vec![Line {
            text: "D-{daysTotal}".into(),
            ..Line::default()
        }];
        let cfg = cfg_with(vec![DisplayOverride {
            id: "MON-A".into(),
            lines: Some(mine.clone()),
            ..DisplayOverride::default()
        }]);
        assert_eq!(effective_for(&cfg, "MON-A").lines, mine);
        assert_eq!(effective_for(&cfg, "MON-B").lines, cfg.lines);
    }
```

(`use crate::config::{Anchor, Config, DisplayOverride, DrawMode, Line};`로 테스트 import를 넓힌다.)

- [ ] **Step 6: 렌더러를 `&[Line]`으로 일반화한다**

`src/render/mod.rs`:

- `LINE_GAP_RATIO`는 그대로, `SUMMARY_RATIO` 상수는 **삭제**한다.
- `pub struct Lines { summary, main }`를 **삭제**한다.
- `use crate::config::{Align, DrawMode, Line, Style};`
- 내부 `Line` 구조체는 `config::Line`과 이름이 겹치므로 `Laid`로 바꾼다.

```rust
/// One line, laid out and ready to draw: its layout, the advance width used to align it,
/// where its ink sits inside its line box (see `ink_span`), and the two per-line settings
/// (`align`, `color`) that survive from the config.
struct Laid {
    layout: IDWriteTextLayout,
    width: f32,
    /// Distance from the layout box's top edge down to the first inked pixel.
    ink_top: f32,
    /// Height of the inked pixels themselves.
    ink_h: f32,
    align: Align,
    color: Option<String>,
}

/// A laid-out stack of lines and the canvas they need. Built once per redraw by `compose`;
/// `size` sizes the composition surface and `paint` draws it. Held by the caller between the
/// two so the (not free -- see `ink_span`) layout work happens once, not twice.
pub struct Composed {
    lines: Vec<Laid>,
    pad: f32,
    gap: f32,
    content_w: f32,
    width: u32,
    height: u32,
}
```

`compose`/`measure`/`line`을 갈아끼운다.

```rust
    pub fn measure(&self, lines: &[Line], style: &Style) -> Result<(u32, u32)> {
        Ok(self.compose(lines, style)?.size())
    }

    /// Lays every line out and sizes the canvas they need. Vertically the canvas holds the
    /// lines' *ink* plus one gap between each adjacent pair, not their line boxes -- see
    /// `ink_span`. A line whose text is empty is dropped: it draws nothing and takes no gap.
    pub fn compose(&self, lines: &[Line], style: &Style) -> Result<Composed> {
        let family = self.text.resolve_family(&style.font_family);

        let mut laid = Vec::with_capacity(lines.len());
        for l in lines {
            if l.text.is_empty() {
                continue;
            }
            laid.push(self.lay(l, &family, style)?);
        }

        let pad = (style.outline_width_px.max(0.0)
            + 4.0
            + if style.shadow { SHADOW_OFFSET } else { 0.0 })
        .ceil();
        // The gap is a fraction of the base size, not of either neighbour's size, so it is
        // the same between every pair however the individual lines are scaled.
        let gap = style.size_px * LINE_GAP_RATIO;

        let content_w = laid.iter().map(|l| l.width).fold(0.0f32, f32::max);
        let content_h = laid.iter().map(|l| l.ink_h).sum::<f32>()
            + gap * laid.len().saturating_sub(1) as f32;

        Ok(Composed {
            lines: laid,
            pad,
            gap,
            content_w,
            width: (content_w + pad * 2.0).ceil().max(1.0) as u32,
            height: (content_h + pad * 2.0).ceil().max(1.0) as u32,
        })
    }

    fn lay(&self, line: &Line, family: &str, style: &Style) -> Result<Laid> {
        // DirectWrite rejects a zero or negative em size; the config validator already keeps
        // size_ratio > 0, so this only guards a pathologically small product.
        let size_px = (style.size_px * line.size_ratio).max(1.0);
        let layout = self.text.layout(&line.text, family, style, size_px)?;
        let (width, box_h) = TextEngine::measure(&layout)?;
        let (ink_top, ink_h) = ink_span(&self.d2d, &layout, box_h)?;
        Ok(Laid {
            layout,
            width,
            ink_top,
            ink_h,
            align: line.align,
            color: line.color.clone(),
        })
    }
```

`paint`는 그대로 두고(클립/Clear/PopAxisAlignedClip), `paint_inner`와 `pass`를 다음으로 바꾼다.

```rust
    /// The actual drawing, run inside the clip pushed by `paint`. Split out so `paint` can
    /// guarantee the clip is popped on every return path, including an error here (see
    /// `PopAxisAlignedClip` above).
    fn paint_inner(
        &self,
        rt: &ID2D1RenderTarget,
        c: &Composed,
        style: &Style,
        ox: f32,
        oy: f32,
        alpha: f32,
    ) -> Result<()> {
        let stroke = self.brush(rt, &style.outline_color, alpha)?;
        let shadow = self.brush(rt, "#000000", SHADOW_ALPHA * alpha)?;

        // Where each line's layout box goes. `DrawTextLayout` takes the box's top-left, but
        // the canvas was sized to the ink, so each line is lifted by the dead space above its
        // own ink (`ink_top`) -- the ink then lands exactly `gap` below the previous line's.
        let mut placed: Vec<(f32, f32, &Laid)> = Vec::with_capacity(c.lines.len());
        let mut ink_y = oy + c.pad;
        for l in &c.lines {
            let x = ox
                + c.pad
                + match l.align {
                    Align::Left => 0.0,
                    Align::Center => (c.content_w - l.width) / 2.0,
                    Align::Right => c.content_w - l.width,
                };
            placed.push((x, ink_y - l.ink_top, l));
            ink_y += l.ink_h + c.gap;
        }

        if style.shadow {
            // Both fill and stroke draw in the shadow colour here, otherwise an outline-mode
            // glyph would show a filled shadow behind a hollow glyph. The per-line colour is
            // ignored for the same reason.
            for (x, y, l) in &placed {
                self.draw_line(
                    rt,
                    l,
                    x + SHADOW_OFFSET,
                    y + SHADOW_OFFSET,
                    style,
                    &shadow,
                    &shadow,
                )?;
            }
        }
        for (x, y, l) in &placed {
            let fill = self.brush(rt, l.color.as_deref().unwrap_or(&style.color), alpha)?;
            self.draw_line(rt, l, *x, *y, style, &fill, &stroke)?;
        }
        Ok(())
    }

    /// One line: `DrawMode::Fill`/`Both` fills with `fill`, `DrawMode::Outline`/`Both` strokes
    /// the glyph outlines with `stroke`.
    fn draw_line(
        &self,
        rt: &ID2D1RenderTarget,
        l: &Laid,
        x: f32,
        y: f32,
        style: &Style,
        fill: &ID2D1SolidColorBrush,
        stroke: &ID2D1SolidColorBrush,
    ) -> Result<()> {
        if matches!(style.mode, DrawMode::Fill | DrawMode::Both) {
            unsafe {
                rt.DrawTextLayout(
                    Vector2 { X: x, Y: y },
                    &l.layout,
                    fill,
                    D2D1_DRAW_TEXT_OPTIONS_NONE,
                );
            }
        }
        if matches!(style.mode, DrawMode::Outline | DrawMode::Both) {
            self.stroke_layout(rt, &l.layout, x, y, stroke, style.outline_width_px)?;
        }
        Ok(())
    }
```

(`#[allow(clippy::too_many_arguments)]`는 `draw_line`에 그대로 붙인다 — 인자 7개.)

- [ ] **Step 7: 렌더러 테스트를 고치고 새로 쓴다**

`src/render/mod.rs`의 `mod tests`에서 `use crate::config::{Align, Line, Style};`로 import를 넓히고, `fn lines()`를 바꾼다.

```rust
    fn lines() -> Vec<Line> {
        crate::config::default_lines(true)
    }

    /// The classic pair, but with the templates already resolved -- the renderer never sees
    /// tokens, `app.rs` substitutes them first.
    fn resolved() -> Vec<Line> {
        vec![
            Line {
                text: "3m 2w 0d".into(),
                size_ratio: 0.28,
                ..Line::default()
            },
            Line {
                text: "2544:18:07".into(),
                ..Line::default()
            },
        ]
    }
```

기존 테스트에서 `&lines()` → `&resolved()`로 바꾸고(`draw`/`measure`의 첫 인자 타입이 `&[Line]`이 된다), `Lines { summary: None, main: ... }`를 쓰던 곳은 `vec![Line { text: ..., ..Line::default() }]`로 바꾼다. `Style { show_summary_line: false, .. }`를 쓰던 테스트(`hiding_the_summary_line_shrinks_the_canvas`, `tabular_figures_keep_the_width_constant_across_digits`, `outline_mode_leaves_glyph_centres_transparent`)는 스타일 대신 줄 리스트로 표현한다.

```rust
    #[test]
    fn dropping_a_line_shrinks_the_canvas() {
        let p = Painter::new().unwrap();
        let tall = p.measure(&resolved(), &Style::default()).unwrap();
        let short = p
            .measure(&resolved()[1..], &Style::default())
            .unwrap();
        assert!(short.1 < tall.1);
    }

    #[test]
    fn an_empty_line_takes_no_room_at_all() {
        let p = Painter::new().unwrap();
        let with_blank = {
            let mut v = resolved();
            v.insert(1, Line::default()); // text: ""
            v
        };
        let a = p.measure(&resolved(), &Style::default()).unwrap();
        let b = p.measure(&with_blank, &Style::default()).unwrap();
        assert_eq!(a, b, "a blank line must not add a gap or any height");
    }

    #[test]
    fn alignment_moves_the_short_line_within_the_canvas() {
        let p = Painter::new().unwrap();
        let style = Style {
            shadow: false,
            outline_width_px: 0.0,
            size_px: 120.0,
            ..Style::default()
        };
        // A short line over a long one, so the short one has room to move.
        let build = |align: Align| {
            vec![
                Line {
                    text: "8".into(),
                    size_ratio: 0.5,
                    align,
                    ..Line::default()
                },
                Line {
                    text: "88888888".into(),
                    ..Line::default()
                },
            ]
        };
        // The x of the topmost inked row tells us where the short line sits.
        let top_ink_x = |align: Align| {
            let (px, w, h) = draw(&p, &build(align), &style);
            let inked_row = (0..h)
                .find(|&y| (0..w).any(|x| px[((y * w + x) * 4 + 3) as usize] != 0))
                .expect("no ink");
            let first = (0..w)
                .find(|&x| px[((inked_row * w + x) * 4 + 3) as usize] != 0)
                .expect("no ink in the first inked row");
            (first, w)
        };
        let (left, w) = top_ink_x(Align::Left);
        let (center, _) = top_ink_x(Align::Center);
        let (right, _) = top_ink_x(Align::Right);
        assert!(left < center, "left {left} should sit left of center {center}");
        assert!(center < right, "center {center} should sit left of right {right}");
        assert!(right < w, "right {right} must stay inside the canvas {w}");
    }

    #[test]
    fn a_per_line_colour_overrides_the_global_one() {
        let p = Painter::new().unwrap();
        let style = Style {
            shadow: false,
            outline_width_px: 0.0,
            color: "#FFFFFF".into(),
            ..Style::default()
        };
        let lines = vec![Line {
            text: "8".into(),
            color: Some("#FF0000".into()),
            ..Line::default()
        }];
        let (px, _, _) = draw(&p, &lines, &style);
        // Premultiplied BGRA: a red line has no blue or green anywhere.
        assert!(
            px.chunks_exact(4).any(|q| q[2] > 0),
            "nothing red was drawn"
        );
        assert!(
            px.chunks_exact(4).all(|q| q[0] == 0 && q[1] == 0),
            "a red line must not put ink in the blue or green channel"
        );
    }

    #[test]
    fn the_gap_is_uniform_across_three_lines() {
        let p = Painter::new().unwrap();
        let size = 200.0;
        let style = Style {
            size_px: size,
            shadow: false,
            outline_width_px: 0.0,
            ..Style::default()
        };
        let lines = vec![
            Line {
                text: "111".into(),
                size_ratio: 0.3,
                ..Line::default()
            },
            Line {
                text: "222".into(),
                size_ratio: 0.5,
                ..Line::default()
            },
            Line {
                text: "333".into(),
                ..Line::default()
            },
        ];
        let (px, w, h) = draw(&p, &lines, &style);
        let gaps = empty_row_runs_between_ink(&px, w, h);
        assert_eq!(gaps.len(), 2, "three lines leave two gaps, got {gaps:?}");
        let want = size * LINE_GAP_RATIO;
        for g in &gaps {
            assert!(
                (*g as f32 - want).abs() <= 2.0,
                "gap {g}px, expected {want}px"
            );
        }
    }
```

`empty_rows_between_lines` 헬퍼를 여러 줄에 맞게 일반화하고, 기존 `the_visible_gap_between_the_lines_is_the_configured_gap`가 이를 쓰게 한다.

```rust
    /// The vertical extents of the fully empty row runs that sit *between* bands of ink.
    /// These are the gaps a person actually sees.
    fn empty_row_runs_between_ink(px: &[u8], w: u32, h: u32) -> Vec<u32> {
        let inked = |y: u32| (0..w).any(|x| px[((y * w + x) * 4 + 3) as usize] != 0);
        let first = (0..h).find(|&y| inked(y)).expect("no ink at all");
        let last = (0..h).rev().find(|&y| inked(y)).expect("no ink at all");

        let mut runs = Vec::new();
        let mut run = 0u32;
        for y in first..=last {
            if inked(y) {
                if run > 0 {
                    runs.push(run);
                    run = 0;
                }
            } else {
                run += 1;
            }
        }
        runs
    }
```

`the_visible_gap_between_the_lines_is_the_configured_gap`의 본문에서 `let gap = empty_rows_between_lines(&px, w, h) as f32;`를 다음으로 바꾼다.

```rust
            let runs = empty_row_runs_between_ink(&px, w, h);
            assert_eq!(runs.len(), 1, "two lines leave one gap, got {runs:?}");
            let gap = runs[0] as f32;
```

- [ ] **Step 8: app.rs를 붙인다**

`src/app.rs`:

```rust
use crate::config::{effective_for, Config, Line};
use crate::countdown::breakdown;
use crate::render::Painter;
use crate::tokens;
```
(`format_main`/`format_summary`/`Lines` import 삭제)

`last_lines` 필드의 타입을 바꾼다.

```rust
    /// The lines actually drawn last time, per panel (a monitor override can give a monitor
    /// its own list). Compared against the freshly resolved ones to skip a redraw that would
    /// paint the same pixels.
    last_lines: Option<Vec<Vec<Line>>>,
```

`render`를 바꾼다.

```rust
    fn render(&mut self) -> Result<()> {
        let now = Zoned::now();
        let b = breakdown(&now, &self.target);

        // Resolve the templates per panel: a monitor override can carry its own line list.
        let resolved: Vec<Vec<Line>> = self
            .panels
            .iter()
            .map(|p| {
                effective_for(&self.cfg, &p.monitor.id)
                    .lines
                    .into_iter()
                    .map(|l| Line {
                        text: tokens::render(&l.text, &b),
                        ..l
                    })
                    .collect()
            })
            .collect();

        if self.last_lines.as_ref() == Some(&resolved) {
            return Ok(());
        }

        tracing::debug!(?resolved, "render");
        if let Err(e) = self.draw(&resolved) {
            tracing::warn!("draw failed, recreating the compositor: {e:#}");
            self.compositor = Compositor::new(self.painter.d2d_factory())?;
            self.rebuild_panels()?; // targets are bound to HWNDs; rebuild them too
            self.draw(&resolved)?; // one retry; a second failure propagates
        }
        self.last_lines = Some(resolved);
        Ok(())
    }
```

`draw`를 바꾼다.

```rust
    fn draw(&mut self, resolved: &[Vec<Line>]) -> Result<()> {
        let workerw = self.workerw;
        let cfg = &self.cfg;
        let painter = &self.painter;
        // Our own windows are not "covering" each other; only a foreign one counts.
        let ours: Vec<HWND> = self.panels.iter().map(|p| p.window.hwnd()).collect();

        for (p, lines) in self.panels.iter_mut().zip(resolved) {
            let eff = effective_for(cfg, &p.monitor.id);
            // Laying the text out is the expensive half of a redraw (glyph outlines, see
            // `render::ink_span`); do it once and let both the sizing and the painting below
            // read from it.
            let composed = painter.compose(lines, &eff.style)?;
            let (w, h) = composed.size();
            let rect = place(
                p.monitor.rect,
                w as i32,
                h as i32,
                eff.anchor,
                eff.offset_px,
            );

            p.window.place(workerw, rect)?;
            // Another wallpaper app may have inserted itself above us since last tick.
            p.window.raise_if_covered(workerw, &ours);

            self.compositor.draw(&mut p.surface, w, h, |rt, origin| {
                painter.paint(rt, &composed, &eff.style, origin)
            })?;
        }
        Ok(())
    }
```

`rebuild_panels`가 이미 `self.last_lines = None`을 하므로 패널 수가 바뀌어도 `zip`이 어긋나지 않는다(리빌드 직후 render가 새로 계산한다).

`src/countdown.rs`에서 `format_main`/`format_summary`를 **삭제**하고, 이를 쓰던 테스트를 토큰으로 바꾼다. 파일 상단 테스트 모듈에 헬퍼를 넣는다.

```rust
    use crate::tokens::render;

    fn main_line(b: &Breakdown) -> String {
        render("{hh}:{mm}:{ss}", b)
    }
    fn summary_line(b: &Breakdown) -> String {
        render("{months}m {weeks}w {days}d", b)
    }
```

그리고 테스트 본문의 `format_main(&b)` → `main_line(&b)`, `format_summary(&b)` → `summary_line(&b)`로 바꾼다(단순 치환, 기대값은 그대로).

- [ ] **Step 9: 설정 창을 컴파일되게 고친다 (편집 UI는 Task 4)**

`src/settings/app.rs`:

- `style_fields`의 마지막 체크박스를 **삭제**한다.

```rust
    changed |= ui
        .checkbox(&mut style.show_summary_line, "Show summary line")
        .changed();
```

- `ui_monitor`에서 합성하는 `Style { ... }`의 `show_summary_line:` 줄을 다음으로 바꾼다(레거시 필드는 이제 아무도 안 읽지만 구조체에는 남아 있다).

```rust
            show_summary_line: None,
```

- 같은 함수의 되쓰기 블록에서 `o.show_summary_line = Some(style.show_summary_line);` 줄을 **삭제**한다.
- `ui_preview`를 줄 리스트 기반으로 바꾼다.

```rust
    /// Approximate desktop preview: an egui-drawn dark panel showing the effective line list
    /// with each line's relative size, colour and alignment. Not a pixel match for the real
    /// DirectWrite renderer (design §4) — just a sense of the composition.
    fn ui_preview(&self, ui: &mut egui::Ui) {
        ui.heading("Preview");

        let eff = self.effective();
        let b = self.preview_breakdown();

        egui::Frame::NONE
            .fill(egui::Color32::from_rgb(24, 24, 24))
            .inner_margin(egui::Margin::same(12))
            .show(ui, |ui| {
                ui.set_min_width(180.0);
                for l in &eff.lines {
                    let text = crate::tokens::render(&l.text, &b);
                    if text.is_empty() {
                        continue;
                    }
                    let hex = l.color.as_deref().unwrap_or(&eff.style.color);
                    let rgb = widgets::hex_to_rgb(hex);
                    // Relative to the base size, not to `size_px`: the preview panel is a
                    // fixed width, so what matters is how the lines relate to each other.
                    let px = (l.size_ratio * PREVIEW_BASE_PX).clamp(8.0, 40.0);
                    let align = match l.align {
                        config::Align::Left => egui::Align::LEFT,
                        config::Align::Center => egui::Align::Center,
                        config::Align::Right => egui::Align::RIGHT,
                    };
                    ui.with_layout(egui::Layout::top_down(align), |ui| {
                        ui.label(
                            egui::RichText::new(text)
                                .size(px)
                                .color(egui::Color32::from_rgb(rgb[0], rgb[1], rgb[2])),
                        );
                    });
                }
            });

        ui.add_space(6.0);
        ui.small("Preview is approximate; see the desktop for the exact result.");
        if !eff.enabled {
            ui.colored_label(
                egui::Color32::from_rgb(220, 160, 60),
                "This monitor is disabled",
            );
        }
    }

    /// The effective config for whatever the user is currently editing.
    fn effective(&self) -> config::Effective {
        let global = || config::Effective {
            enabled: true,
            anchor: self.cfg.layout.anchor,
            offset_px: self.cfg.layout.offset_px,
            style: self.cfg.style.clone(),
            lines: self.cfg.lines.clone(),
        };
        match self.target {
            Target::Global => global(),
            Target::Monitor(i) => match self.monitors.get(i) {
                Some(m) => config::effective_for(&self.cfg, &m.id),
                None => global(),
            },
        }
    }

    /// The countdown the preview renders, from the real target/now so the preview is a genuine
    /// (if approximately rendered) countdown, not a static mock. Falls back to a zeroed
    /// breakdown if `target` cannot be resolved in the local time zone (e.g. an out-of-range
    /// year) rather than panicking.
    fn preview_breakdown(&self) -> crate::countdown::Breakdown {
        match self.cfg.target.to_zoned(jiff::tz::TimeZone::system()) {
            Ok(target) => crate::countdown::breakdown(&jiff::Zoned::now(), &target),
            Err(_) => crate::countdown::breakdown(&jiff::Zoned::now(), &jiff::Zoned::now()),
        }
    }
```

`preview_lines`는 **삭제**한다. 파일 상단 상수에 추가한다.

```rust
/// The preview's font size for a line whose `size_ratio` is 1.0. The preview shows the lines'
/// relative sizes, not their real pixel sizes -- the panel is far smaller than a monitor.
const PREVIEW_BASE_PX: f32 = 28.0;
```

`src/settings/overrides.rs`: `has_style_override`의 OR 체인에서 `o.show_summary_line.is_some()`을 `o.lines.is_some()`으로 바꾸고, `enable_style_override`의 `o.show_summary_line = Some(g_style.show_summary_line);`를 다음으로 바꾼다.

```rust
    let g_lines = cfg.lines.clone();
    // ...
    o.lines = Some(g_lines);
```

(`let g_style = cfg.style.clone();` 옆에 `let g_lines = cfg.lines.clone();`를 두고, `o.show_summary_line = ...` 자리에 `o.lines = Some(g_lines);`를 넣는다.)

`disable_style_override`에서 `o.show_summary_line = None;` → `o.lines = None;`.

테스트의 14-setter 배열에서 `|o| o.show_summary_line = Some(true),`를 바꾼다.

```rust
            |o| o.lines = Some(crate::config::default_lines(true)),
```

- [ ] **Step 10: 전체 테스트를 돌린다**

Run: `cargo test`
Expected: 전부 PASS. 실패하면 위 단계에서 놓친 호출부를 고친다.

- [ ] **Step 11: 실제로 돌려본다**

```bash
cargo build --release
```

`%APPDATA%\DesktopCountdown\config.toml`을 백업해 두고, 앱을 실행해 바탕화면이 지금과 똑같이 나오는지, `config.toml`에 `[[line]]` 블록 두 개가 생겼는지 확인한다.

- [ ] **Step 12: 커밋**

```bash
git add src/
git commit -m "화면 구성을 줄 리스트로 일반화

- [[line]] 리스트(text/size_ratio/align/color)로 요약줄 특수 케이스 제거
- 렌더러가 줄 목록을 순회하며 정렬·줄별 색 지원
- 레거시 show_summary_line은 첫 로드 때 기본 리스트로 마이그레이션
- countdown::format_main/format_summary 삭제, 토큰 템플릿으로 대체"
```

---

### Task 3: 프리셋과 줄 리스트 조작 (`src/settings/lines.rs`)

**Files:**
- Create: `src/settings/lines.rs`
- Modify: `src/settings/mod.rs`

**Interfaces:**
- Consumes: `config::{Line, Align, SUMMARY_TEMPLATE, MAIN_TEMPLATE, SUMMARY_SIZE_RATIO}`
- Produces:
  - `pub struct Preset { pub name: &'static str, pub lines: &'static [(&'static str, f32)] }`
  - `pub const PRESETS: [Preset; 5]`
  - `impl Preset { pub fn build(&self) -> Vec<Line> }`
  - `pub fn move_up(lines: &mut [Line], i: usize)`
  - `pub fn move_down(lines: &mut [Line], i: usize)`
  - `pub fn remove(lines: &mut Vec<Line>, i: usize)` — 줄이 하나뿐이면 아무것도 안 한다
  - `pub fn add(lines: &mut Vec<Line>)`

- [ ] **Step 1: 실패하는 테스트를 쓴다**

`src/settings/lines.rs`:

```rust
//! Pure line-list editing for the settings window: presets and reordering. No egui.

use crate::config::{Align, Line, MAIN_TEMPLATE, SUMMARY_SIZE_RATIO, SUMMARY_TEMPLATE};

#[cfg(test)]
mod tests {
    use super::*;

    fn l(text: &str) -> Line {
        Line {
            text: text.into(),
            ..Line::default()
        }
    }

    #[test]
    fn the_classic_preset_is_the_default_list() {
        let classic = PRESETS
            .iter()
            .find(|p| p.name == "Classic")
            .expect("Classic preset");
        assert_eq!(classic.build(), crate::config::default_lines(true));
    }

    #[test]
    fn every_preset_builds_at_least_one_line_and_uses_the_defaults_elsewhere() {
        for p in &PRESETS {
            let lines = p.build();
            assert!(!lines.is_empty(), "{} built nothing", p.name);
            for line in &lines {
                assert!(line.size_ratio > 0.0);
                assert_eq!(line.align, Align::Center);
                assert_eq!(line.color, None);
            }
        }
    }

    #[test]
    fn move_up_swaps_with_the_line_above() {
        let mut v = vec![l("a"), l("b")];
        move_up(&mut v, 1);
        assert_eq!(v[0].text, "b");
        assert_eq!(v[1].text, "a");
    }

    #[test]
    fn move_up_on_the_first_line_does_nothing() {
        let mut v = vec![l("a"), l("b")];
        move_up(&mut v, 0);
        assert_eq!(v[0].text, "a");
    }

    #[test]
    fn move_down_swaps_with_the_line_below() {
        let mut v = vec![l("a"), l("b")];
        move_down(&mut v, 0);
        assert_eq!(v[0].text, "b");
    }

    #[test]
    fn move_down_on_the_last_line_does_nothing() {
        let mut v = vec![l("a"), l("b")];
        move_down(&mut v, 1);
        assert_eq!(v[1].text, "b");
    }

    #[test]
    fn remove_drops_the_line() {
        let mut v = vec![l("a"), l("b")];
        remove(&mut v, 0);
        assert_eq!(v.len(), 1);
        assert_eq!(v[0].text, "b");
    }

    /// An empty list means "not configured" (`config::migrate` refills it), so the editor
    /// must never produce one.
    #[test]
    fn remove_refuses_to_empty_the_list() {
        let mut v = vec![l("only")];
        remove(&mut v, 0);
        assert_eq!(v.len(), 1, "the last line must survive");
    }

    #[test]
    fn add_appends_a_blank_line_at_the_base_size() {
        let mut v = vec![l("a")];
        add(&mut v);
        assert_eq!(v.len(), 2);
        assert_eq!(v[1], Line::default());
    }

    #[test]
    fn out_of_range_indices_are_ignored() {
        let mut v = vec![l("a"), l("b")];
        move_up(&mut v, 9);
        move_down(&mut v, 9);
        remove(&mut v, 9);
        assert_eq!(v.len(), 2);
    }
}
```

- [ ] **Step 2: 실패를 확인한다**

Run: `cargo test --lib settings::lines`
Expected: 컴파일 실패 — `PRESETS`, `move_up` 등 없음

- [ ] **Step 3: 구현한다**

테스트 모듈 위에 넣는다.

```rust
/// A named line list the user can drop in with one click. Only text and size differ between
/// presets; alignment and colour stay at their defaults, which the user then tweaks.
pub struct Preset {
    pub name: &'static str,
    /// `(text, size_ratio)` per line, top to bottom.
    pub lines: &'static [(&'static str, f32)],
}

impl Preset {
    pub fn build(&self) -> Vec<Line> {
        self.lines
            .iter()
            .map(|(text, size_ratio)| Line {
                text: (*text).to_string(),
                size_ratio: *size_ratio,
                align: Align::Center,
                color: None,
            })
            .collect()
    }
}

pub const PRESETS: [Preset; 5] = [
    Preset {
        name: "Classic",
        lines: &[(SUMMARY_TEMPLATE, SUMMARY_SIZE_RATIO), (MAIN_TEMPLATE, 1.0)],
    },
    Preset {
        name: "Clock only",
        lines: &[(MAIN_TEMPLATE, 1.0)],
    },
    Preset {
        name: "D-Day",
        lines: &[("D-{daysTotal}", 1.0), (MAIN_TEMPLATE, 0.3)],
    },
    Preset {
        name: "Days left",
        lines: &[("{daysTotal} days left", 0.35), (MAIN_TEMPLATE, 1.0)],
    },
    Preset {
        name: "Caption + Clock",
        lines: &[
            ("Deadline", 0.25),
            (MAIN_TEMPLATE, 1.0),
            ("{daysTotal} days left", 0.25),
        ],
    },
];

pub fn move_up(lines: &mut [Line], i: usize) {
    if i > 0 && i < lines.len() {
        lines.swap(i - 1, i);
    }
}

pub fn move_down(lines: &mut [Line], i: usize) {
    if i + 1 < lines.len() {
        lines.swap(i, i + 1);
    }
}

/// Drops line `i`, unless it is the only one: an empty list reads as "not configured" and
/// `config::migrate` would refill it with the classic pair on the next load.
pub fn remove(lines: &mut Vec<Line>, i: usize) {
    if lines.len() > 1 && i < lines.len() {
        lines.remove(i);
    }
}

pub fn add(lines: &mut Vec<Line>) {
    lines.push(Line::default());
}
```

`src/settings/mod.rs`에 모듈을 등록한다 (`mod overrides;` 옆).

```rust
mod lines;
```

- [ ] **Step 4: 통과를 확인한다**

Run: `cargo test --lib settings::lines`
Expected: 10 tests PASS

- [ ] **Step 5: 커밋**

```bash
git add src/settings/lines.rs src/settings/mod.rs
git commit -m "설정 창 줄 리스트 조작과 프리셋 정의

- Classic/Clock only/D-Day/Days left/Caption + Clock 5종 프리셋
- 줄 이동·삭제·추가 (마지막 한 줄은 삭제 불가)"
```

---

### Task 4: 설정 창의 줄 편집 UI

**Files:**
- Modify: `src/settings/app.rs`

**Interfaces:**
- Consumes: `settings::lines::{PRESETS, add, move_down, move_up, remove}`, `crate::tokens::TOKENS`, `config::{Align, Line}`
- Produces: `fn lines_editor(ui: &mut egui::Ui, lines: &mut Vec<Line>, salt: &str) -> bool` (파일 프라이빗)

- [ ] **Step 1: 줄 편집 위젯을 만든다**

`src/settings/app.rs`의 `style_fields` 아래에 추가한다.

```rust
fn align_label(align: Align) -> &'static str {
    match align {
        Align::Left => "Left",
        Align::Center => "Center",
        Align::Right => "Right",
    }
}

/// The line-list editor: a preset picker, the token reference, and one row per line. Shared by
/// the global list and a monitor override's list (`salt` keeps their widget ids apart).
/// Returns whether anything changed this frame.
fn lines_editor(ui: &mut egui::Ui, lines: &mut Vec<Line>, salt: &str) -> bool {
    let mut changed = false;

    // Presets replace the whole list, and the settings window saves on change with no undo, so
    // picking one in the combo must not be enough -- the user has to press Apply.
    let preset_id = egui::Id::new(("dc_preset", salt));
    let mut chosen: usize = ui.ctx().data(|d| d.get_temp(preset_id)).unwrap_or(0);
    ui.horizontal(|ui| {
        ui.label("Preset:");
        egui::ComboBox::from_id_salt(("dc_preset_combo", salt))
            .selected_text(lines::PRESETS[chosen].name)
            .show_ui(ui, |ui| {
                for (i, p) in lines::PRESETS.iter().enumerate() {
                    ui.selectable_value(&mut chosen, i, p.name);
                }
            });
        if ui.button("Apply").clicked() {
            *lines = lines::PRESETS[chosen].build();
            changed = true;
        }
    });
    ui.ctx().data_mut(|d| d.insert_temp(preset_id, chosen));

    egui::CollapsingHeader::new("Available tokens")
        .id_salt(("dc_tokens", salt))
        .show(ui, |ui| {
            egui::Grid::new(("dc_token_grid", salt))
                .num_columns(2)
                .spacing([12.0, 2.0])
                .show(ui, |ui| {
                    for (token, description) in crate::tokens::TOKENS {
                        ui.monospace(token);
                        ui.small(description);
                        ui.end_row();
                    }
                });
        });

    let last = lines.len().saturating_sub(1);
    let mut action: Option<(usize, LineAction)> = None;
    for (i, line) in lines.iter_mut().enumerate() {
        ui.horizontal(|ui| {
            changed |= ui
                .add(
                    egui::TextEdit::singleline(&mut line.text)
                        .desired_width(180.0)
                        .hint_text("Text or {token}"),
                )
                .changed();
            changed |= ui
                .add(
                    egui::DragValue::new(&mut line.size_ratio)
                        .speed(0.01)
                        .range(0.05..=4.0)
                        .fixed_decimals(2),
                )
                .changed();
            egui::ComboBox::from_id_salt(("dc_align", salt, i))
                .width(70.0)
                .selected_text(align_label(line.align))
                .show_ui(ui, |ui| {
                    for a in [Align::Left, Align::Center, Align::Right] {
                        changed |= ui.selectable_value(&mut line.align, a, align_label(a)).changed();
                    }
                });

            // The colour button needs a concrete colour even while the line inherits one, so
            // the checkbox is what distinguishes "inherit" from "this exact colour".
            let mut own = line.color.is_some();
            if ui.checkbox(&mut own, "").on_hover_text("Own colour").changed() {
                line.color = own.then(|| "#FFFFFF".to_string());
                changed = true;
            }
            if let Some(hex) = &mut line.color {
                let mut rgb = widgets::hex_to_rgb(hex);
                if ui.color_edit_button_srgb(&mut rgb).changed() {
                    *hex = widgets::rgb_to_hex(rgb);
                    changed = true;
                }
            }

            if ui
                .add_enabled(i > 0, egui::Button::new("\u{2191}"))
                .clicked()
            {
                action = Some((i, LineAction::Up));
            }
            if ui
                .add_enabled(i < last, egui::Button::new("\u{2193}"))
                .clicked()
            {
                action = Some((i, LineAction::Down));
            }
            if ui
                .add_enabled(last > 0, egui::Button::new("\u{2715}"))
                .on_hover_text("Remove this line")
                .clicked()
            {
                action = Some((i, LineAction::Remove));
            }
        });
    }

    // Applied after the loop: reordering or removing mid-iteration would invalidate the
    // indices the rest of the loop is walking.
    if let Some((i, what)) = action {
        match what {
            LineAction::Up => lines::move_up(lines, i),
            LineAction::Down => lines::move_down(lines, i),
            LineAction::Remove => lines::remove(lines, i),
        }
        changed = true;
    }

    if ui.button("+ Add line").clicked() {
        lines::add(lines);
        changed = true;
    }

    changed
}

enum LineAction {
    Up,
    Down,
    Remove,
}
```

파일 상단 import에 추가한다.

```rust
use crate::config::{self, Align, Anchor, Config, DrawMode, Line, Style};
use crate::settings::{lines, overrides, widgets};
```

- [ ] **Step 2: 전역 편집 화면에 붙인다**

`ui_global`에서 `style_fields(...)` 호출 블록 뒤, `ui.separator(); ui.heading("Layout");` 앞에 넣는다.

```rust
        ui.separator();

        ui.heading("Lines");
        let mut lines = std::mem::take(&mut self.cfg.lines);
        let changed = lines_editor(ui, &mut lines, "global");
        self.cfg.lines = lines;
        if changed {
            self.mark_dirty();
        }
```

(`self.cfg`를 통째로 빌려주면 `&mut self` 충돌이 나므로 잠시 `take`했다 되돌린다.)

- [ ] **Step 3: 모니터 오버라이드 화면에 붙인다**

`ui_monitor`의 `style_fields(...)` 되쓰기 블록 뒤(함수 끝)에 넣는다.

```rust
        ui.separator();

        ui.heading("Lines");
        let mut lines = self.cfg.displays[o_idx]
            .lines
            .clone()
            .unwrap_or_else(|| self.cfg.lines.clone());
        if lines_editor(ui, &mut lines, "monitor") {
            self.cfg.displays[o_idx].lines = Some(lines);
            self.mark_dirty();
        }
```

- [ ] **Step 4: 컴파일과 테스트**

Run: `cargo test`
Expected: 전부 PASS

- [ ] **Step 5: 설정 창을 직접 열어 확인한다**

```bash
cargo run --release -- --settings
```

확인할 것:
1. Lines 섹션에 두 줄(요약, 시계)이 보이고, 미리보기가 그 구성을 그린다.
2. `+ Add line` → 빈 줄 추가 → 텍스트에 `수능까지` 입력 → 미리보기와 바탕화면에 즉시 나온다.
3. `↑`/`↓`로 순서를 바꾸면 미리보기 순서가 바뀐다.
4. 줄이 하나만 남으면 `✕`가 비활성.
5. Preset을 `D-Day`로 고르고 Apply → 리스트가 통째로 교체된다.
6. Available tokens 패널에 토큰 13개가 보인다.
7. 색 체크박스를 켜면 색 버튼이 나오고, 그 줄만 색이 바뀐다.
8. 모니터 탭에서 "Override for this monitor"를 켜면 전역 줄 리스트가 복사되어 편집 가능하고, 그 모니터만 바뀐다.

- [ ] **Step 6: 커밋**

```bash
git add src/settings/app.rs
git commit -m "설정 창에 줄 편집 UI 추가

- 줄별 텍스트/크기 비율/정렬/색(상속 토글) 편집, 순서 변경, 추가·삭제
- 프리셋 드롭다운(Apply로 적용)과 토큰 도움말 패널
- 미리보기가 줄 리스트를 비율 크기·색·정렬로 근사"
```

---

### Task 5: 문서와 최종 검증

**Files:**
- Modify: `README.md`

- [ ] **Step 1: README의 "설정" 절을 고친다**

`## 설정` 절의 본문을 다음으로 교체한다(트레이/파일 경로 문단은 유지).

```markdown
## 설정

트레이 아이콘 우클릭 → "설정 열기"로 GUI 설정 창을 엽니다(창 UI는 영어). 마감 시각(`target`),
글꼴·크기·색상 등 스타일, 화면에 표시할 줄 목록, 배치(anchor/offset), 모니터별 오버라이드를 창에서
편집하며, 변경하면 자동으로 `config.toml`에 저장되고 바탕화면에 약 0.2초 안에 반영됩니다.

### 줄 목록과 토큰

화면은 위에서 아래로 쌓이는 줄들로 구성됩니다. 각 줄은 토큰이 들어갈 수 있는 템플릿 문자열이고,
줄마다 크기 비율(`[style].size_px`에 곱해집니다)·정렬·색을 지정할 수 있습니다. 색을 지정하지 않으면
`[style].color`를 따릅니다.

```toml
[[line]]
text = "{daysTotal} days left"
size_ratio = 0.3

[[line]]
text = "{hh}:{mm}:{ss}"
size_ratio = 1.0

[[line]]
text = "수능까지 화이팅"
size_ratio = 0.2
align = "right"
color = "#AAAAAA"
```

쓸 수 있는 토큰:

| 토큰 | 의미 |
|---|---|
| `{months}` `{weeks}` `{days}` | 달력 기준 개월/주/일 |
| `{daysTotal}` `{hoursTotal}` `{minutesTotal}` `{secondsTotal}` | 남은 총 일/시/분/초 |
| `{hours}` `{minutes}` `{seconds}` | 하루 안의 시(0–23), 분, 초 |
| `{hh}` `{mm}` `{ss}` | 2자리 제로패딩 (`{hh}`는 총 시간) |

정의되지 않은 토큰은 그대로 화면에 나옵니다(오타를 바로 알 수 있게).

설정 창의 Preset 드롭다운으로 자주 쓰는 구성(Classic, Clock only, D-Day, Days left,
Caption + Clock)을 한 번에 넣을 수 있습니다. 기본값은 Classic —
`{months}m {weeks}w {days}d` 위에 `{hh}:{mm}:{ss}`입니다.
```

첫 문단의 화면 예시(`3m 2w 0d` / `2544:18:07`) 아래 설명도 "기본 구성"임을 밝히도록 한 문장 고친다.

```markdown
아랫줄이 남은 총 시간, 윗줄은 개월/주/일 요약입니다(기본 구성 — 줄 목록과 표시 형식은 설정에서
자유롭게 바꿀 수 있습니다). 데스크톱 아이콘 아래, Wallpaper Engine 같은
```

`주요 항목은 ...` 문단에 줄 목록을 넣는다.

```markdown
주요 항목은 `target`(마감 시각), `[style]`의 `font_family`·`size_px`·`color`·`mode`,
`[[line]]`(표시할 줄들), `[layout]`의 `anchor`·`offset_px`입니다.
```

- [ ] **Step 2: 전체 검증**

```bash
cargo test
cargo clippy --all-targets -- -D warnings
cargo build --release
```

Expected: 테스트 전부 통과, clippy 경고 없음, 빌드 성공

- [ ] **Step 3: 기존 설정 파일 마이그레이션을 실제로 확인한다**

`%APPDATA%\DesktopCountdown\config.toml`을 이 브랜치 이전 형식(즉 `[[line]]` 없이
`show_summary_line = false`)으로 바꿔 두고 앱을 실행한다. 시계 한 줄만 나오고, 설정 창을 열어
아무 값이나 건드리면 파일에 `[[line]]` 한 개가 쓰이며 `show_summary_line`이 사라져야 한다.

- [ ] **Step 4: 커밋**

```bash
git add README.md
git commit -m "README: 줄 목록과 토큰 설명 추가"
```

---

## 자체 검토

**설계 문서 대응:**

| 설계 절 | 태스크 |
|---|---|
| §2 데이터 모델 (`Line`, `Align`, 검증) | Task 2 Step 1–4 |
| §3 토큰 | Task 1 |
| §4 렌더러 | Task 2 Step 6–7 |
| §5 마이그레이션 | Task 2 Step 3 (`migrate`), Step 5 (io), Task 5 Step 3 (실물 확인) |
| §6 모니터별 오버라이드 | Task 2 Step 5 (merge), Step 9 (overrides.rs), Task 4 Step 3 (UI) |
| §7 설정 창 (프리셋/토큰 도움말/줄 편집/미리보기) | Task 3, Task 4 |
| §8 영향받는 파일 | 전부 태스크에 배정됨 |
| §9 비목표 | 구현하지 않음 |
